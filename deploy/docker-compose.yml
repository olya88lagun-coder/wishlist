name: wishlist

services:
  web:
    image: ghcr.io/${GHCR_OWNER}/wishlist-web:latest
    env_file: .env
    restart: unless-stopped
    mem_limit: 300m
    networks:
      shared:
        aliases: [wishlist-web]

  worker:
    image: ghcr.io/${GHCR_OWNER}/wishlist-worker:latest
    env_file: .env
    restart: unless-stopped
    mem_limit: 200m
    stop_grace_period: 30s
    # egress с IPv6: по IPv4 сервера Wildberries отвечает антиботом (498)
    networks: [shared, egress]

  migrate:
    image: ghcr.io/${GHCR_OWNER}/wishlist-migrate:latest
    env_file: .env
    profiles: ["tools"]
    mem_limit: 200m
    networks: [shared]

networks:
  shared:
    external: true
    name: food-tracker-bot_default
  egress:
    enable_ipv6: true
